# 📱 للمطور - ابدأ هنا

## 🎯 المطلوب

تطبيق جوال للعملاء فقط (Customer App)
- لوحة التحكم (Admin) تبقى على الويب
- الوكلاء (Agents) يستخدمون الويب
- مدراء النقاط (Point Managers) يستخدمون الويب

---

## 📁 الملفات المهمة

### 1. **MOBILE_APP_CUSTOMER_API.md**
التوثيق الكامل لجميع الـ APIs التي يحتاجها تطبيق العميل

### 2. **Customer_App_API.postman_collection.json**
Postman Collection جاهز للاستيراد والاختبار

### 3. **API_DOCUMENTATION.md**
التوثيق الكامل لجميع الـ APIs (للمرجع)

---

## 🚀 البدء السريع

### الخطوة 1: اختبر الـ API
1. افتح Postman
2. استورد ملف `Customer_App_API.postman_collection.json`
3. الـ `baseUrl` مضبوط على `http://192.168.1.111:5000/api`
4. جرب Login وخذ الـ Token
5. ضع الـ Token في متغير `token`
6. جرب باقي الـ endpoints

**⚠️ مهم للجوال:**
- للجوال الحقيقي: استخدم `http://192.168.1.111:5000/api`
- للمحاكي Android: استخدم `http://10.0.2.2:5000/api`
- للمحاكي iOS: استخدم `http://localhost:5000/api`
- تأكد أن الجوال والكمبيوتر على نفس الشبكة (WiFi)!

### الخطوة 2: افهم التطبيق
اقرأ ملف `MOBILE_APP_CUSTOMER_API.md` لفهم:
- الشاشات المطلوبة
- الـ APIs لكل شاشة
- أمثلة الكود

### الخطوة 3: ابدأ التطوير
استخدم React Native أو Flutter أو أي تقنية تفضلها

---

## 📱 الشاشات المطلوبة

### 1. التسجيل وتسجيل الدخول ✅
- شاشة Splash
- شاشة Onboarding (اختياري)
- شاشة Login
- شاشة Register
- شاشة Forgot Password (اختياري)

### 2. الصفحة الرئيسية ✅
- عرض المتاجر المدعومة (Amazon, Noon, AliExpress, إلخ)
- حقل إدخال رابط المنتج
- زر "جلب المنتج"
- عرض الكوبونات النشطة
- قائمة جانبية (Drawer/Menu)

### 3. تفاصيل المنتج ✅
- عرض صورة المنتج
- اسم المنتج
- السعر الأصلي
- حساب التكلفة الإجمالية (شحن + عمولة + جمارك)
- اختيار الكمية
- اختيار الخيارات (لون، مقاس، إلخ)
- زر "إضافة للسلة"

### 4. السلة ✅
- عرض المنتجات في السلة
- تعديل الكمية
- حذف منتج
- عرض التكلفة الإجمالية
- زر "إتمام الطلب"

### 5. إتمام الطلب (Checkout) ✅
- إدخال عنوان الشحن
- اختيار طريقة الدفع
- إدخال كود الخصم (اختياري)
- ملاحظات (اختياري)
- زر "تأكيد الطلب"

### 6. الطلبات ✅
- قائمة جميع الطلبات
- فلترة حسب الحالة (pending, processing, shipped, delivered)
- عرض تفاصيل الطلب
- زر "تتبع الطلب"

### 7. تتبع الطلب ✅
- رقم الطلب
- الحالة الحالية
- Timeline (خط زمني للطلب)
- رقم التتبع (إن وجد)
- تاريخ التسليم المتوقع

### 8. المحفظة ✅
- عرض الرصيد الحالي
- زر "شحن المحفظة"
- إدخال كود الشحن
- سجل المعاملات

### 9. نقاط البيع ✅
- عرض أقرب نقطة بيع (باستخدام GPS)
- قائمة جميع نقاط البيع
- تفاصيل نقطة البيع (عنوان، هاتف، ساعات العمل)
- خريطة (Google Maps)

### 10. الدعم ✅
- الدردشة مع الدعم
- إرسال رسالة
- عرض الرسائل السابقة

### 11. الملف الشخصي ✅
- عرض بيانات المستخدم
- تعديل البيانات
- تغيير كلمة المرور
- تسجيل الخروج

### 12. الإعدادات (اختياري) ✅
- اللغة (عربي/إنجليزي)
- الإشعارات
- عن التطبيق
- سياسة الخصوصية
- الشروط والأحكام

---

## 🔑 المصادقة (Authentication)

### تسجيل الدخول:
```javascript
POST /api/auth/login
Body: {
  "email": "user@example.com",
  "password": "password123"
}

Response: {
  "success": true,
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": { ... }
}
```

### استخدام الـ Token:
```javascript
// احفظ الـ Token
await AsyncStorage.setItem('token', data.token);

// استخدمه في كل طلب
headers: {
  'Authorization': `Bearer ${token}`
}
```

---

## 🛍️ تدفق الشراء (Purchase Flow)

1. **المستخدم يدخل رابط المنتج**
   ```
   POST /api/products/fetch-from-url
   ```

2. **عرض تفاصيل المنتج وحساب التكلفة**
   ```
   POST /api/products/calculate-cost
   ```

3. **إضافة المنتج للسلة**
   ```
   POST /api/cart/fetch-and-add
   ```

4. **عرض السلة**
   ```
   GET /api/cart
   ```

5. **إتمام الطلب**
   ```
   POST /api/cart/checkout
   ```

6. **الدفع (Stripe)**
   - افتح `paymentUrl` في WebView أو المتصفح
   - بعد الدفع، تحقق من حالة الطلب

7. **عرض الطلب**
   ```
   GET /api/orders/{orderId}
   ```

---

## 💡 نصائح مهمة

### 1. إدارة الحالة (State Management)
استخدم:
- Redux أو Context API (React Native)
- Provider (Flutter)
- أي state management تفضله

### 2. التخزين المحلي
احفظ:
- الـ Token
- بيانات المستخدم
- السلة (اختياري)

### 3. معالجة الأخطاء
```javascript
try {
  const response = await fetch(url, options);
  const data = await response.json();
  
  if (!data.success) {
    // عرض رسالة الخطأ للمستخدم
    Alert.alert('خطأ', data.message);
  }
} catch (error) {
  // خطأ في الاتصال
  Alert.alert('خطأ', 'تحقق من اتصالك بالإنترنت');
}
```

### 4. التحقق من الـ Token
```javascript
// عند فتح التطبيق
const token = await AsyncStorage.getItem('token');

if (token) {
  // تحقق من صلاحية الـ Token
  const response = await fetch('/api/auth/me', {
    headers: { 'Authorization': `Bearer ${token}` }
  });
  
  if (response.status === 401) {
    // الـ Token منتهي الصلاحية
    // اطلب من المستخدم تسجيل الدخول
  }
}
```

### 5. الإشعارات (Push Notifications)
- استخدم Firebase Cloud Messaging
- سجل الـ Device Token في السيرفر (سيتم إضافة endpoint لاحقاً)

### 6. الخرائط (Maps)
- استخدم Google Maps API
- احصل على موقع المستخدم (GPS)
- اعرض أقرب نقطة بيع

---

## 🎨 التصميم (UI/UX)

### الألوان الأساسية:
- Primary: `#d946ef` (وردي/بنفسجي)
- Secondary: `#0ea5e9` (أزرق)
- Background: `#ffffff` (أبيض)
- Text: `#1f2937` (رمادي غامق)

### الخطوط:
- العربية: Cairo, Tajawal
- الإنجليزية: Inter, Roboto

### الاتجاه:
- RTL (من اليمين لليسار) للعربية
- LTR (من اليسار لليمين) للإنجليزية

---

## 📦 المكتبات المقترحة (React Native)

```json
{
  "dependencies": {
    "react-native": "latest",
    "react-navigation": "latest",
    "@react-native-async-storage/async-storage": "latest",
    "axios": "latest",
    "react-native-maps": "latest",
    "react-native-webview": "latest",
    "react-native-vector-icons": "latest"
  }
}
```

---

## ✅ Checklist

- [ ] قرأت التوثيق الكامل
- [ ] جربت الـ API في Postman
- [ ] فهمت نظام المصادقة
- [ ] فهمت تدفق الشراء
- [ ] جربت جميع الـ endpoints الأساسية
- [ ] جاهز للبدء! 🚀

---

## 📞 الدعم

إذا كان لديك أي استفسار:
1. راجع `MOBILE_APP_CUSTOMER_API.md`
2. جرب الـ API في Postman
3. تواصل مع فريق التطوير

---

## 🎯 الهدف النهائي

تطبيق جوال احترافي للعملاء يتيح لهم:
- ✅ طلب منتجات من المتاجر العالمية
- ✅ تتبع طلباتهم
- ✅ إدارة محفظتهم
- ✅ التواصل مع الدعم
- ✅ العثور على أقرب نقطة بيع

---

**بالتوفيق في التطوير! 🚀**

**آخر تحديث:** 9 يناير 2025
