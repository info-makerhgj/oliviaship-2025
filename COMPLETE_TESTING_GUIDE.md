# 🚀 دليل التشغيل الكامل - من الصفر حتى التشغيل

## 📋 الخطوات الكاملة (3 خطوات فقط!)

### ✅ الطريقة السريعة (Windows):

#### انقر نقراً مزدوجاً على:
```
START_EVERYTHING.bat
```

**سيتم فتح 3 نوافذ تلقائياً:**
1. الخادم (Server)
2. Metro Bundler
3. Android

---

### ✅ الطريقة اليدوية (خطوة بخطوة):

#### الخطوة 1: تشغيل الخادم

**Terminal 1:**
```bash
# انقر نقراً مزدوجاً على:
START_SERVER.bat

# أو يدوياً:
cd server
npm install  # إذا لم تكن مثبتة
npm start
```

**النتيجة المتوقعة:**
```
✅ Connected to MongoDB
🚀 Server running on port 5000
```

**⚠️ اترك هذا Terminal مفتوحاً!**

---

#### الخطوة 2: تشغيل Metro Bundler

**Terminal 2 (جديد):**
```bash
# انقر نقراً مزدوجاً على:
mobile-app/OliviaShipApp/RUN_APP.bat

# أو يدوياً:
cd mobile-app/OliviaShipApp
npm install  # إذا لم تكن مثبتة
npm start -- --reset-cache
```

**النتيجة المتوقعة:**
```
Metro waiting on...
```

**⚠️ اترك هذا Terminal مفتوحاً أيضاً!**

---

#### الخطوة 3: تشغيل التطبيق

**Terminal 3 (جديد):**
```bash
# انقر نقراً مزدوجاً على:
mobile-app/OliviaShipApp/RUN_ANDROID.bat

# أو يدوياً:
cd mobile-app/OliviaShipApp
npm run android
```

**النتيجة المتوقعة:**
- يفتح Android Emulator أو يستخدم الجهاز المتصل
- يبدأ بناء التطبيق (قد يستغرق دقائق)
- يفتح التطبيق تلقائياً

---

## ✅ التحقق من أن كل شيء يعمل:

### 1. الخادم ✅
- Terminal 1 يعرض: `Server running on port 5000`
- لا توجد أخطاء حمراء

### 2. Metro Bundler ✅
- Terminal 2 يعرض: `Metro waiting on...`
- لا توجد أخطاء

### 3. التطبيق ✅
- يفتح على المحاكي/الجهاز
- يظهر شاشة Login
- لا توجد أخطاء حمراء في التطبيق

---

## 🧪 اختبار سريع (دقيقتان):

### 1. تسجيل الدخول:
- [ ] فتح التطبيق → يظهر Login
- [ ] إدخال البريد والكلمة
- [ ] الضغط "تسجيل الدخول"
- [ ] ينتقل للـ Dashboard ✅

### 2. التنقل:
- [ ] Dashboard يظهر
- [ ] الضغط على "Orders" → يعمل
- [ ] الضغط على "Wallet" → يعمل
- [ ] الضغط على "Profile" → يعمل

### 3. الميزات:
- [ ] الضغط على "طلب منتج جديد"
- [ ] إدخال رابط منتج
- [ ] إضافة للسلة
- [ ] فتح السلة → المنتج موجود

---

## 🐛 حل المشاكل السريع:

### ❌ "Server not running"
```bash
# Terminal 1
cd server
npm start
```

### ❌ "Metro bundler failed"
```bash
# Terminal 2
cd mobile-app/OliviaShipApp
npm start -- --reset-cache
```

### ❌ "Network request failed"
1. تحقق من IP في `src/services/api.js` → `192.168.1.111:5000`
2. تأكد من أن الخادم يعمل
3. تأكد من أن الجهاز/المحاكي على نفس WiFi

### ❌ "Android build failed"
```bash
cd mobile-app/OliviaShipApp/android
gradlew clean
cd ..
npm run android
```

### ❌ "Unable to resolve module"
```bash
cd mobile-app/OliviaShipApp
npm install
```

---

## 📱 ملاحظات مهمة:

### Android Emulator:
- **إذا استخدمت Emulator**: قد تحتاج تغيير IP إلى `10.0.2.2` في `api.js`
- **إذا استخدمت جهاز حقيقي**: استخدم `192.168.1.111`

### أول مرة:
- قد يستغرق البناء الأول 5-10 دقائق
- تأكد من أن Android Studio مثبت
- تأكد من أن Android SDK مثبت

---

## 🎯 الترتيب الصحيح:

```
┌─────────────────────────────────────┐
│  Terminal 1: الخادم                │
│  cd server && npm start             │
│  ✅ Server running on port 5000    │
└─────────────────────────────────────┘
           │
           ├──────────────────────────┐
           │                          │
┌──────────▼──────────┐  ┌───────────▼──────────┐
│ Terminal 2: Metro   │  │ Terminal 3: Android  │
│ npm start           │  │ npm run android      │
│ ✅ Metro waiting    │  │ ✅ App opens         │
└─────────────────────┘  └─────────────────────┘
```

---

## ✅ Checklist النهائي:

- [ ] Terminal 1: الخادم يعمل
- [ ] Terminal 2: Metro Bundler يعمل
- [ ] Terminal 3: التطبيق يفتح
- [ ] شاشة Login تظهر
- [ ] تسجيل الدخول يعمل
- [ ] Dashboard يظهر
- [ ] لا توجد أخطاء حمراء

---

## 🚀 الملفات الجاهزة:

1. **START_EVERYTHING.bat** - تشغيل كل شيء دفعة واحدة
2. **START_SERVER.bat** - تشغيل الخادم فقط
3. **RUN_APP.bat** - تشغيل Metro Bundler فقط
4. **RUN_ANDROID.bat** - تشغيل Android فقط

---

**جاهز للبدء!** 🎉

**نصيحة:** ابدأ بـ `START_EVERYTHING.bat` لأسهل طريقة!






