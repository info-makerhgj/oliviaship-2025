# 📦 دليل بناء APK لتطبيق أوليفيا شيب

## 🎯 الطريقة الأولى: EAS Build (الأسهل - موصى بها)

### 1. تثبيت EAS CLI:
```bash
npm install -g eas-cli
```

### 2. تسجيل الدخول لحساب Expo:
```bash
eas login
```
إذا ما عندك حساب، سجل من هنا: https://expo.dev/signup

### 3. إعداد المشروع:
```bash
cd OliviaShip-Expo-App
eas build:configure
```

### 4. بناء APK:
```bash
eas build -p android --profile preview
```

سيطلب منك:
- ✅ اختر "Generate new keystore"
- ✅ انتظر حتى ينتهي البناء (5-10 دقائق)
- ✅ سيعطيك رابط لتحميل APK

---

## 🎯 الطريقة الثانية: Expo Build (قديمة لكن تعمل)

### 1. تسجيل الدخول:
```bash
expo login
```

### 2. بناء APK:
```bash
cd OliviaShip-Expo-App
expo build:android -t apk
```

### 3. تحميل APK:
بعد انتهاء البناء، سيعطيك رابط لتحميل الملف.

---

## 🎯 الطريقة الثالثة: بناء محلي (للمطورين)

### المتطلبات:
- ✅ Android Studio مثبت
- ✅ Java JDK 17
- ✅ Android SDK

### الخطوات:

#### 1. تثبيت المكتبات:
```bash
cd OliviaShip-Expo-App
npm install
```

#### 2. Pre-build:
```bash
npx expo prebuild
```

#### 3. بناء APK:
```bash
cd android
gradlew assembleRelease
```

#### 4. الملف سيكون في:
```
android/app/build/outputs/apk/release/app-release.apk
```

---

## 📱 اختبار APK:

### على الجوال:
1. انقل ملف APK للجوال
2. افتح الملف
3. اسمح بالتثبيت من مصادر غير معروفة
4. ثبت التطبيق

### على المحاكي:
```bash
adb install app-release.apk
```

---

## ⚙️ إعدادات مهمة:

### تغيير اسم التطبيق:
في `app.json`:
```json
"name": "أوليفيا شيب"
```

### تغيير الأيقونة:
استبدل الملفات في مجلد `assets/`:
- `icon.png` (1024x1024)
- `adaptive-icon.png` (1024x1024)
- `splash-icon.png` (1284x2778)

### تغيير Package Name:
في `app.json`:
```json
"android": {
  "package": "com.oliviaship.app"
}
```

---

## 🚀 الطريقة الموصى بها (EAS):

```bash
# 1. تثبيت EAS
npm install -g eas-cli

# 2. تسجيل الدخول
eas login

# 3. الذهاب للمشروع
cd OliviaShip-Expo-App

# 4. إعداد EAS
eas build:configure

# 5. بناء APK
eas build -p android --profile preview
```

بعد 5-10 دقائق، سيعطيك رابط لتحميل APK! 🎉

---

## 📝 ملاحظات:

- ✅ الطريقة الأولى (EAS) هي الأسهل والأسرع
- ✅ لا تحتاج Android Studio
- ✅ البناء يتم على سيرفرات Expo
- ✅ مجاني للاستخدام الشخصي

---

## 🆘 حل المشاكل:

### "eas: command not found"
```bash
npm install -g eas-cli
```

### "Not logged in"
```bash
eas login
```

### "Build failed"
تأكد من:
- ✅ ملف `app.json` صحيح
- ✅ جميع الصور موجودة في `assets/`
- ✅ `package.json` يحتوي على جميع المكتبات

---

## 🎯 الخطوة التالية:

بعد تحميل APK، يمكنك:
1. ✅ تثبيته على جوالك
2. ✅ مشاركته مع الآخرين
3. ✅ رفعه على Google Play Store

للنشر على Google Play:
```bash
eas build -p android --profile production
```
