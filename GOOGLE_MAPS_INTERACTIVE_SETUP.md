# 🗺️ تفعيل الخريطة التفاعلية لتحديد الموقع

## المميزات الجديدة

✅ **خريطة تفاعلية قابلة للنقر**
- انقر على أي مكان في الخريطة لتعيين الدبوس
- اسحب الدبوس لموقع آخر
- الإحداثيات تُحدَّث تلقائياً في الحقول

✅ **تحديث تلقائي**
- عند النقر على الخريطة، تُحدَّث الإحداثيات مباشرة
- لا حاجة لإدخال الإحداثيات يدوياً

## 📋 خطوات التفعيل

### 1. الحصول على Google Maps API Key

1. اذهب إلى [Google Cloud Console](https://console.cloud.google.com/)
2. أنشئ مشروع جديد أو اختر مشروع موجود
3. اذهب إلى **APIs & Services** → **Library**
4. ابحث عن **"Maps JavaScript API"**
5. اضغط **Enable**
6. اذهب إلى **APIs & Services** → **Credentials**
7. اضغط **+ CREATE CREDENTIALS** → **API Key**
8. انسخ المفتاح

### 2. تفعيل API المطلوبة

يجب تفعيل:
- ✅ **Maps JavaScript API** (للخريطة التفاعلية)
- ✅ **Geocoding API** (للبحث عن العناوين) - اختياري

### 3. إضافة المفتاح في `index.html`

افتح `index.html` وافك التعليق عن السطر:

```html
<script src="https://maps.googleapis.com/maps/api/js?key=YOUR_API_KEY&libraries=places&language=ar" async defer></script>
```

استبدل `YOUR_API_KEY` بمفتاحك.

**مثال:**
```html
<script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXX&libraries=places&language=ar" async defer></script>
```

### 4. إعادة تشغيل الخادم

```bash
npm run dev
```

## 🎯 طريقة الاستخدام

### بدون API Key (وضع القراءة فقط):
- الخريطة تظهر كـ iframe (للعرض فقط)
- يجب إدخال الإحداثيات يدوياً من Google Maps

### مع API Key (وضع تفاعلي):
1. افتح نموذج إضافة/تعديل نقطة
2. أدخل العنوان (اختياري)
3. **انقر على الخريطة** لتعيين موقع الدبوس
4. **أو اسحب الدبوس** لموقع آخر
5. الإحداثيات تُحدَّث تلقائياً! ✅

## 💡 نصائح

- **API Key مجاني** لحد 200$ شهرياً (كافي لـ 28,000 طلب خريطة)
- يمكن تقييد المفتاح بنطاق الموقع فقط (Domain restriction)
- الخريطة تعمل بدون API key لكن بشكل iframe فقط

## ⚠️ ملاحظات

- إذا لم يتم تحميل API، النظام يستخدم iframe كبديل
- رسالة تحذير تظهر في حالة عدم وجود API key
- جميع المميزات الأخرى تعمل بدون API key

## 🔒 الأمان

**لإنتاج آمن:**
1. قيِّد API Key بنطاق موقعك فقط
2. فعّل **Application restrictions**
3. استخدم **HTTP referrers** لتقييد الاستخدام

---

**الآن يمكنك النقر على الخريطة مباشرة لتحديد الموقع! 🎉**


