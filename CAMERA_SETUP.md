# 📷 إعداد الكاميرا لمسح QR Code على الجوال

## ⚠️ المشكلة الشائعة

عند محاولة مسح QR Code على الجوال، قد تواجه رسالة: **"فشل في فتح الكاميرا. يرجى التحقق من الصلاحيات."**

## 🔧 الحلول

### 1. إعطاء الصلاحيات في المتصفح

#### Chrome (Android/iOS):
1. افتح الموقع `http://192.168.1.111:5173`
2. اضغط على أيقونة **القفل** 🔒 أو **المعلومات** (i) في شريط العنوان
3. ابحث عن **"الكاميرا"** أو **"Camera"**
4. اختر **"السماح"** أو **"Allow"**
5. أغلق القائمة وارجع للصفحة
6. اضغط على زر **"إعادة المحاولة"**

#### Firefox (Android):
1. اضغط على أيقونة **الدرع** 🛡️ في شريط العنوان
2. اضغط على **"إعدادات الموقع"**
3. ابحث عن **"الكاميرا"**
4. اختر **"السماح"**
5. اضغط **"إعادة المحاولة"**

#### Safari (iOS):
1. اذهب إلى **إعدادات** → **Safari**
2. اضغط على **"الكاميرا"**
3. اختر **"اسمح"** للموقع
4. أو أثناء استخدام الموقع، اضغط على أيقونة **AA** في شريط العنوان → **إعدادات الموقع** → **الكاميرا** → **اسمح**

### 2. التأكد من HTTPS (مهم للجوال)

بعض المتصفحات على الجوال ترفض الكاميرا على HTTP. الحلول:

#### الحل الأسهل (للاختبار):
- استخدم **Chrome** على الجوال - يدعم الكاميرا على HTTP في الشبكة المحلية

#### الحل الأفضل (للإنتاج):
- استخدم HTTPS للموقع
- أو استخدم تطبيق مثل **ngrok** لإنشاء HTTPS tunnel:
  ```bash
  npx ngrok http 5173
  ```
- استخدم الرابط الذي يعطيك إياه ngrok (يبدأ بـ https://)

### 3. إغلاق التطبيقات الأخرى

- أغلق التطبيقات التي تستخدم الكاميرا (Instagram, Snapchat, Zoom, إلخ)
- أعد المحاولة

### 4. تحديث المتصفح

- تأكد من استخدام أحدث نسخة من المتصفح
- Chrome: أحدث نسخة
- Firefox: أحدث نسخة

### 5. اختبار الكاميرا

- افتح تطبيق الكاميرا العادي وتأكد أنها تعمل
- إذا لم تعمل، قد تكون المشكلة في الجهاز نفسه

## 🧪 اختبار سريع

1. افتح: `http://192.168.1.111:5173/dashboard/wallet`
2. اضغط على **"مسح QR Code"**
3. إذا طلب المتصفح الإذن → اضغط **"السماح"**
4. يجب أن تفتح الكاميرا

## 📝 ملاحظات مهمة

- ✅ **HTTP على الشبكة المحلية يعمل** مع Chrome/Firefox على معظم الأجهزة
- ⚠️ **HTTPS مطلوب** للإنتاج أو مع بعض المتصفحات الصارمة
- ✅ **الصلاحيات تُطلب تلقائياً** عند الضغط على "مسح QR Code"
- ✅ **زر "إعادة المحاولة"** يظهر عند فشل الكاميرا

## 🆘 إذا استمرت المشكلة

1. تحديث الصفحة (F5)
2. إعادة تشغيل المتصفح
3. محاولة متصفح آخر (Chrome → Firefox أو العكس)
4. التحقق من إعدادات الجهاز → التطبيقات → المتصفح → الصلاحيات → الكاميرا


