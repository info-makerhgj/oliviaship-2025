{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "setup": {
          "nixPkgs": ["nodejs_20"]
        },
        "install": {
          "cmds": ["npm install --production --ignore-scripts"]
        }
      },
      "start": {
        "cmd": "node server/minimal-server.js"
      }
    }
  },
  "deploy": {
    "startCommand": "node server/minimal-server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
