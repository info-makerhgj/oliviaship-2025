# 🚀 دليل التشغيل الكامل - خطوة بخطوة

## 📋 المتطلبات

- [x] Node.js مثبت
- [x] React Native CLI
- [x] Android Studio (لـ Android) أو Xcode (لـ iOS)
- [x] الخادم يعمل على `192.168.1.111:5000`

---

## 🎯 الخطوات الكاملة

### الخطوة 1: تشغيل الخادم

#### الطريقة 1: باستخدام ملف BAT (Windows)
```bash
# انقر نقراً مزدوجاً على:
START_SERVER.bat
```

#### الطريقة 2: يدوياً
```bash
cd server
npm install  # إذا لم تكن التبعيات مثبتة
npm start
```

**النتيجة المتوقعة:**
```
✅ Connected to MongoDB
🚀 Server running on port 5000
```

---

### الخطوة 2: تشغيل Metro Bundler

#### الطريقة 1: باستخدام ملف BAT
```bash
# انقر نقراً مزدوجاً على:
mobile-app/OliviaShipApp/RUN_APP.bat
```

#### الطريقة 2: يدوياً
```bash
cd mobile-app/OliviaShipApp
npm start -- --reset-cache
```

**النتيجة المتوقعة:**
```
Metro waiting on...
```

**⚠️ مهم:** اترك هذا Terminal مفتوحاً!

---

### الخطوة 3: تشغيل التطبيق

#### على Android:

##### الطريقة 1: باستخدام ملف BAT
```bash
# في terminal جديد، انقر نقراً مزدوجاً على:
mobile-app/OliviaShipApp/RUN_ANDROID.bat
```

##### الطريقة 2: يدوياً
```bash
# في terminal جديد
cd mobile-app/OliviaShipApp
npm run android
```

**النتيجة المتوقعة:**
- يفتح Android Emulator أو يستخدم الجهاز المتصل
- يبدأ بناء التطبيق
- يفتح التطبيق تلقائياً

#### على iOS (Mac فقط):
```bash
cd mobile-app/OliviaShipApp
cd ios && pod install && cd ..
npm run ios
```

---

## ✅ التحقق من التشغيل

### 1. الخادم يعمل:
افتح المتصفح على: `http://192.168.1.111:5000/api/health`
يجب أن ترى استجابة JSON

### 2. Metro Bundler يعمل:
يجب أن ترى في Terminal:
```
Metro waiting on...
```

### 3. التطبيق يعمل:
- يفتح على المحاكي/الجهاز
- يظهر شاشة Login
- لا توجد أخطاء حمراء

---

## 🧪 اختبار سريع

### 1. تسجيل الدخول:
- أدخل البريد والكلمة
- اضغط "تسجيل الدخول"
- يجب أن ينتقل للـ Dashboard

### 2. الصفحات:
- [ ] Dashboard يظهر
- [ ] Orders يعمل
- [ ] Wallet يعمل
- [ ] Profile يعمل

### 3. الميزات:
- [ ] طلب منتج جديد
- [ ] إضافة للسلة
- [ ] عرض السلة

---

## 🐛 حل المشاكل

### المشكلة 1: "Server not running"
**الحل:**
```bash
cd server
npm start
```

### المشكلة 2: "Metro bundler failed"
**الحل:**
```bash
cd mobile-app/OliviaShipApp
npm start -- --reset-cache
```

### المشكلة 3: "Network request failed"
**الحل:**
1. تحقق من IP في `src/services/api.js` → `192.168.1.111:5000`
2. تأكد من أن الخادم يعمل
3. تأكد من أن الجهاز/المحاكي على نفس الشبكة

### المشكلة 4: "Android build failed"
**الحل:**
```bash
cd mobile-app/OliviaShipApp/android
./gradlew clean
cd ..
npm run android
```

### المشكلة 5: "Unable to resolve module"
**الحل:**
```bash
cd mobile-app/OliviaShipApp
npm install
```

---

## 📱 ملاحظات مهمة

### Android Emulator:
- إذا استخدمت Emulator، قد تحتاج `10.0.2.2` بدلاً من `192.168.1.111`
- عدّل في `api.js` إذا لزم الأمر

### جهاز حقيقي:
- تأكد من أن الجهاز على نفس WiFi
- قد تحتاج تعطيل Firewall مؤقتاً

### iOS Simulator:
- استخدم `localhost` أو IP الحقيقي
- تأكد من أن Simulator على نفس الشبكة

---

## 🎯 الترتيب الصحيح

```
1. Terminal 1: تشغيل الخادم
   └─> cd server && npm start

2. Terminal 2: تشغيل Metro Bundler
   └─> cd mobile-app/OliviaShipApp && npm start -- --reset-cache

3. Terminal 3: تشغيل التطبيق
   └─> cd mobile-app/OliviaShipApp && npm run android
```

---

## ✅ Checklist النهائي

- [ ] الخادم يعمل على البورت 5000
- [ ] Metro Bundler يعمل
- [ ] التطبيق يفتح على الجهاز/المحاكي
- [ ] شاشة Login تظهر
- [ ] تسجيل الدخول يعمل
- [ ] Dashboard يظهر
- [ ] لا توجد أخطاء حمراء

---

**جاهز للبدء!** 🚀

**ملاحظة:** إذا واجهت أي مشكلة، راجع قسم "حل المشاكل" أعلاه.






