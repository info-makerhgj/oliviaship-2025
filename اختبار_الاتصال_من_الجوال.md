# 📱 اختبار الاتصال من الجوال

## ✅ الخطوات السريعة

### 1. تأكد أن السيرفر يعمل
افتح Command Prompt وشغل:
```bash
cd C:\Users\cct33\Downloads\2yam
npm run server
```

يجب أن ترى:
```
🚀 Server running on port 5000
🌐 Frontend: http://localhost:5173
📱 Access from mobile: http://YOUR_IP:5000
```

---

### 2. تأكد من عنوان IP
عنوان IP الحالي:
```
192.168.1.111
```

إذا تغير، احصل على العنوان الجديد:
```bash
ipconfig
```
ابحث عن `IPv4 Address` تحت `Wireless LAN adapter Wi-Fi`

---

### 3. اختبر من متصفح الجوال
1. افتح متصفح Chrome أو Safari على الجوال
2. اذهب إلى:
   ```
   http://192.168.1.111:5000/api/settings
   ```
3. يجب أن ترى بيانات JSON

**إذا ظهرت البيانات = الاتصال يعمل! ✅**

---

### 4. اختبر تسجيل الدخول
في Postman على الجوال أو من المتصفح:

```
POST http://192.168.1.111:5000/api/auth/login
Content-Type: application/json

{
  "email": "admin@oliviaship.com",
  "password": "admin123"
}
```

---

## 🔧 حل المشاكل

### المشكلة: "لا يمكن الوصول للموقع"

#### الحل 1: تأكد من الشبكة
- الجوال والكمبيوتر على نفس WiFi؟
- جرب فتح `http://192.168.1.111:5173` (الموقع الرئيسي)

#### الحل 2: جدار الحماية
1. افتح Windows Defender Firewall
2. اضغط "Allow an app through firewall"
3. ابحث عن Node.js وفعّله
4. أو أوقف جدار الحماية مؤقتاً للاختبار

#### الحل 3: أعد تشغيل السيرفر
```bash
# أوقف السيرفر (Ctrl+C)
# ثم شغله مرة أخرى
npm run server
```

---

## 📱 للمطور

### عنوان الـ API للجوال:
```javascript
const API_URL = 'http://192.168.1.111:5000/api';
```

### للمحاكي Android:
```javascript
const API_URL = 'http://10.0.2.2:5000/api';
```

### للمحاكي iOS:
```javascript
const API_URL = 'http://localhost:5000/api';
```

---

## 🧪 كود اختبار للتطبيق

```javascript
// TestAPI.js
import React, { useEffect, useState } from 'react';
import { View, Text, Button, StyleSheet } from 'react-native';

const API_URL = 'http://192.168.1.111:5000/api';

const TestAPI = () => {
  const [status, setStatus] = useState('جاري الاختبار...');
  const [details, setDetails] = useState('');

  const testConnection = async () => {
    setStatus('جاري الاختبار...');
    setDetails('');

    try {
      // اختبار 1: الاتصال الأساسي
      const response = await fetch(`${API_URL}/settings`, {
        method: 'GET',
        headers: {
          'Content-Type': 'application/json',
        },
      });

      if (!response.ok) {
        throw new Error(`HTTP ${response.status}`);
      }

      const data = await response.json();

      if (data.success) {
        setStatus('✅ الاتصال يعمل بنجاح!');
        setDetails(`اسم الموقع: ${data.settings.general.siteName}`);
      } else {
        setStatus('❌ خطأ في البيانات');
        setDetails(JSON.stringify(data));
      }
    } catch (error) {
      setStatus('❌ فشل الاتصال');
      setDetails(`الخطأ: ${error.message}\n\nتأكد من:\n1. السيرفر يعمل\n2. الجوال والكمبيوتر على نفس WiFi\n3. عنوان IP صحيح`);
    }
  };

  const testLogin = async () => {
    setStatus('جاري اختبار تسجيل الدخول...');
    setDetails('');

    try {
      const response = await fetch(`${API_URL}/auth/login`, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({
          email: 'admin@oliviaship.com',
          password: 'admin123',
        }),
      });

      const data = await response.json();

      if (data.success) {
        setStatus('✅ تسجيل الدخول نجح!');
        setDetails(`المستخدم: ${data.user.name}\nالدور: ${data.user.role}`);
      } else {
        setStatus('❌ فشل تسجيل الدخول');
        setDetails(data.message);
      }
    } catch (error) {
      setStatus('❌ خطأ في الاتصال');
      setDetails(error.message);
    }
  };

  useEffect(() => {
    testConnection();
  }, []);

  return (
    <View style={styles.container}>
      <Text style={styles.title}>اختبار الاتصال بالـ API</Text>
      
      <Text style={styles.url}>API URL:</Text>
      <Text style={styles.urlText}>{API_URL}</Text>
      
      <Text style={styles.status}>{status}</Text>
      
      {details ? (
        <Text style={styles.details}>{details}</Text>
      ) : null}
      
      <View style={styles.buttons}>
        <Button title="اختبار الاتصال" onPress={testConnection} />
        <View style={styles.spacer} />
        <Button title="اختبار تسجيل الدخول" onPress={testLogin} />
      </View>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    backgroundColor: '#fff',
    justifyContent: 'center',
  },
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    textAlign: 'center',
    marginBottom: 20,
  },
  url: {
    fontSize: 14,
    color: '#666',
    marginBottom: 5,
  },
  urlText: {
    fontSize: 12,
    color: '#0ea5e9',
    marginBottom: 20,
  },
  status: {
    fontSize: 18,
    textAlign: 'center',
    marginBottom: 20,
    fontWeight: 'bold',
  },
  details: {
    fontSize: 14,
    color: '#666',
    textAlign: 'center',
    marginBottom: 20,
    padding: 10,
    backgroundColor: '#f5f5f5',
    borderRadius: 5,
  },
  buttons: {
    marginTop: 20,
  },
  spacer: {
    height: 10,
  },
});

export default TestAPI;
```

---

## ✅ Checklist

- [ ] السيرفر يعمل (`npm run server`)
- [ ] الجوال والكمبيوتر على نفس WiFi
- [ ] فتحت `http://192.168.1.111:5000/api/settings` في متصفح الجوال
- [ ] ظهرت البيانات بنجاح
- [ ] جربت تسجيل الدخول
- [ ] جاهز للتطوير! 🚀

---

**عنوان IP الحالي:** `192.168.1.111`
**آخر تحديث:** 9 يناير 2025
