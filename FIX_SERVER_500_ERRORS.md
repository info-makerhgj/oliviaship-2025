# 🔧 إصلاح أخطاء 500 في الخادم

## ❌ الأخطاء الحالية:

```
500 Internal Server Error في:
- /api/chat/my-chat
- /api/payments/stats
- /api/payments
- /api/auth/me
- /api/cart
- /api/orders
- /api/pos
- /api/smart-cart-orders
```

---

## ✅ الخطوات للإصلاح:

### 1. تحقق من MongoDB

```bash
# في Terminal:
CHECK_SERVER_STATUS.bat

# أو يدوياً:
netstat -an | findstr ":27017"
```

**إذا لم يكن MongoDB يعمل:**
```bash
# Windows (إذا كان مثبت كخدمة):
net start MongoDB

# أو شغله يدوياً من مجلد التثبيت
```

---

### 2. تحقق من الخادم

```bash
cd server
npm start
```

**راقب الأخطاء في Terminal:**
- إذا رأيت: `❌ MongoDB connection error` → MongoDB غير متصل
- إذا رأيت: `✅ Connected to MongoDB` → MongoDB متصل

---

### 3. فحص ملف .env

تحقق من وجود ملف `.env` في مجلد `server/`:

```env
MONGODB_URI=mongodb://localhost:27017/yemen-delivery
JWT_SECRET=your-secret-key
NODE_ENV=development
PORT=5000
```

---

### 4. إعادة تشغيل الخادم

```bash
# أوقف الخادم (Ctrl+C)
# ثم:
cd server
npm start
```

---

## 🐛 الأسباب المحتملة:

### 1. MongoDB غير متصل
- **الحل:** تأكد من تشغيل MongoDB
- **التحقق:** `netstat -an | findstr ":27017"`

### 2. MongoDB URI خاطئ
- **الحل:** تحقق من `MONGODB_URI` في `.env`

### 3. قاعدة البيانات غير موجودة
- **الحل:** MongoDB سينشئها تلقائياً عند الاتصال

### 4. خطأ في الـ Controllers
- **الحل:** راقب سجلات الخادم لمعرفة الخطأ الفعلي

---

## 📝 سجلات الأخطاء:

**في Terminal (حيث يعمل الخادم):**
- ستجد تفاصيل الأخطاء
- ابحث عن: `❌ Error Details:`
- ابحث عن: `❌ MongoDB connection error`

---

## ✅ بعد الإصلاح:

1. ✅ تأكد من أن MongoDB يعمل
2. ✅ تأكد من أن الخادم يعمل (`http://localhost:5000`)
3. ✅ افتح الموقع (`http://localhost:5173`)
4. ✅ جرب تسجيل الدخول
5. ✅ راقب Console في المتصفح

---

**ابدأ بـ:** `CHECK_SERVER_STATUS.bat`






