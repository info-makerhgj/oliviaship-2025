# 🚀 كيف ترقي خطة AWS Lightsail - خطوة بخطوة

## 📋 الخطوات بالصور والشرح:

---

## ⚠️ مهم جداً: خذ نسخة احتياطية أولاً!

### الخطوة 1: أخذ Snapshot (نسخة احتياطية)

1. **افتح AWS Lightsail Console:**
   - اذهب إلى: https://lightsail.aws.amazon.com/
   - سجل دخول

2. **اختر Instance الخاص بك:**
   - اضغط على `oliviaship-backend`

3. **اذهب إلى Snapshots:**
   - اضغط على تبويب `Snapshots` في الأعلى
   - أو اضغط على `Manage` ثم `Snapshots`

4. **أنشئ Snapshot:**
   - اضغط على زر `+ Create snapshot`
   - اسم الـ Snapshot: `before-upgrade-2025`
   - اضغط `Create`
   - **انتظر 5-10 دقائق** حتى يكتمل

5. **تحقق من Snapshot:**
   - يجب أن تشوف الـ Snapshot بحالة `Available`
   - لونه أخضر ✅

---

## 🔄 الخطوة 2: ترقية الخطة

### الطريقة الأولى: Upgrade مباشر (الأسهل)

⚠️ **ملاحظة:** بعض المناطق لا تدعم Upgrade مباشر، إذا ما لقيت الخيار، استخدم الطريقة الثانية.

1. **ارجع لصفحة Instance:**
   - اضغط على اسم Instance: `oliviaship-backend`

2. **اضغط على النقاط الثلاث:**
   - في أعلى اليمين، اضغط على `⋮` (ثلاث نقاط عمودية)
   - أو اضغط على `Actions`

3. **اختر Change plan:**
   - اضغط على `Change plan` أو `Upgrade`

4. **اختر الخطة الجديدة:**
   - اختر: **$5/month** (1 GB RAM, 1 vCPU, 40 GB SSD)
   - أو: **$10/month** (2 GB RAM, 1 vCPU, 60 GB SSD) - إذا تبغى أكثر

5. **تأكيد الترقية:**
   - اضغط `Change plan` أو `Confirm`
   - **انتظر 5-15 دقيقة** حتى تكتمل الترقية

6. **تحقق من الترقية:**
   - في صفحة Instance، شوف:
     - RAM: يجب أن تكون `1 GB` (أو 2 GB)
     - Storage: يجب أن تكون `40 GB` (أو 60 GB)

---

### الطريقة الثانية: إنشاء Instance جديد من Snapshot (الأفضل)

إذا ما لقيت خيار "Change plan"، استخدم هذه الطريقة:

#### 1️⃣ إنشاء Instance جديد:

1. **اذهب إلى Snapshots:**
   - في Lightsail Console
   - اضغط على `Snapshots` في القائمة اليسرى

2. **اختر Snapshot:**
   - اختر الـ Snapshot اللي أخذته: `before-upgrade-2025`
   - اضغط على النقاط الثلاث `⋮`
   - اختر `Create new instance`

3. **اختر المنطقة:**
   - **Region:** Bahrain (me-south-1) ⚠️ مهم جداً!
   - **Availability Zone:** أي واحدة

4. **اختر الخطة:**
   - اختر: **$5/month** (1 GB RAM, 40 GB SSD)
   - أو: **$10/month** (2 GB RAM, 60 GB SSD)

5. **اسم Instance:**
   - Name: `oliviaship-backend-v2`

6. **إنشاء:**
   - اضغط `Create instance`
   - **انتظر 5-10 دقائق** حتى يصبح `Running`

#### 2️⃣ نقل Static IP:

1. **اذهب إلى Networking:**
   - في Lightsail Console
   - اضغط على `Networking` في القائمة اليسرى

2. **اختر Static IP:**
   - اختر الـ Static IP الحالي
   - اضغط عليه

3. **Detach من Instance القديم:**
   - اضغط `Detach`
   - تأكيد

4. **Attach إلى Instance الجديد:**
   - اضغط `Attach to an instance`
   - اختر: `oliviaship-backend-v2`
   - اضغط `Attach`

#### 3️⃣ اختبار Instance الجديد:

```bash
# SSH إلى Instance الجديد:
ssh -i your-key.pem ubuntu@YOUR_IP

# تحقق من المساحة:
df -h /
# يجب أن تشوف 40GB (أو 60GB)

# تحقق من الذاكرة:
free -h
# يجب أن تشوف 1GB (أو 2GB)

# تحقق من التطبيق:
pm2 status
pm2 logs oliviaship
```

#### 4️⃣ حذف Instance القديم:

⚠️ **فقط بعد التأكد أن الجديد يشتغل!**

1. **في Lightsail Console:**
   - اختر Instance القديم: `oliviaship-backend`
   - اضغط النقاط الثلاث `⋮`
   - اختر `Delete`
   - تأكيد الحذف

---

## 📦 الخطوة 3: إعادة تثبيت المكتبات

بعد الترقية، ارجع للسيرفر وثبت المكتبات:

```bash
# 1. SSH إلى السيرفر:
ssh -i your-key.pem ubuntu@YOUR_IP

# 2. تحقق من المساحة والذاكرة:
df -h /
free -h

# 3. إيقاف التطبيق:
pm2 stop all
pm2 delete all

# 4. حذف node_modules القديمة:
cd ~/oliviaship-2025
rm -rf node_modules
rm -rf package-lock.json

# 5. تنظيف:
npm cache clean --force

# 6. تثبيت كل المكتبات (مع dev dependencies):
npm install

# 7. تشغيل التطبيق:
pm2 start server/index.js --name oliviaship
pm2 save

# 8. التحقق:
pm2 logs oliviaship
```

---

## 📊 مقارنة الخطط:

| الخطة | RAM | CPU | المساحة | السعر | التوصية |
|-------|-----|-----|---------|-------|---------|
| الحالية | 512MB | 1 vCPU | 20GB | $3.50 | ❌ غير كافية |
| **$5** | **1GB** | **1 vCPU** | **40GB** | **$5** | ✅ **موصى به** |
| $10 | 2GB | 1 vCPU | 60GB | $10 | ⭐ للمشاريع الكبيرة |
| $20 | 4GB | 2 vCPU | 80GB | $20 | ⭐⭐ للمشاريع الضخمة |

---

## 🎯 التوصية:

### للمشروع الحالي:
**خطة $5/شهر** (1GB RAM, 40GB SSD)
- ✅ كافية لتثبيت كل المكتبات
- ✅ أداء جيد
- ✅ سعر معقول

### إذا كان المشروع كبير أو عندك ملفات كثيرة:
**خطة $10/شهر** (2GB RAM, 60GB SSD)
- ✅ أداء أفضل
- ✅ مساحة أكبر
- ✅ يتحمل ضغط أكبر

---

## ⚠️ ملاحظات مهمة:

### 1. خذ Snapshot دائماً قبل أي تغيير:
- نسخة احتياطية تحميك من أي مشكلة
- يمكنك الرجوع لها بسهولة

### 2. لا تحذف Instance القديم إلا بعد التأكد:
- اختبر Instance الجديد أولاً
- تأكد أن كل شيء يشتغل
- بعدها احذف القديم

### 3. Static IP مهم:
- لا تنسى نقله للـ Instance الجديد
- وإلا Frontend ما بيقدر يتصل بـ API

### 4. بعد الترقية:
- ثبت كل المكتبات بـ `npm install` (بدون --production)
- تأكد أن كل شيء يشتغل
- راقب الأداء

---

## 🚀 الخطوات السريعة (ملخص):

### الطريقة السهلة:
1. ✅ خذ Snapshot
2. ✅ اضغط `⋮` → `Change plan`
3. ✅ اختر $5/month
4. ✅ انتظر 10 دقائق
5. ✅ أعد تثبيت المكتبات

### الطريقة الأفضل:
1. ✅ خذ Snapshot
2. ✅ أنشئ Instance جديد من Snapshot
3. ✅ اختر $5/month
4. ✅ انقل Static IP
5. ✅ اختبر
6. ✅ احذف القديم

---

## 📞 إذا واجهت مشكلة:

### المشكلة: ما لقيت "Change plan"
**الحل:** استخدم الطريقة الثانية (إنشاء Instance جديد)

### المشكلة: الترقية فشلت
**الحل:** ارجع للـ Snapshot وأنشئ Instance جديد

### المشكلة: Static IP ما انتقل
**الحل:** 
1. Detach من القديم
2. Attach للجديد
3. انتظر دقيقة

### المشكلة: التطبيق ما يشتغل بعد الترقية
**الحل:**
```bash
pm2 stop all
pm2 delete all
cd ~/oliviaship-2025
rm -rf node_modules
npm install
pm2 start server/index.js --name oliviaship
pm2 save
```

---

## ✅ بعد الترقية:

### تحقق من:
1. ✅ المساحة: `df -h /` → يجب أن تكون 40GB
2. ✅ الذاكرة: `free -h` → يجب أن تكون 1GB
3. ✅ التطبيق: `pm2 status` → يجب أن يكون `online`
4. ✅ API: `curl http://YOUR_IP:5000/api/health`

### النتيجة المتوقعة:
- ✅ كل المكتبات مثبتة
- ✅ التطبيق يشتغل بدون أخطاء
- ✅ الأداء أفضل
- ✅ المساحة كافية

---

## 🎉 الخلاصة:

### المشكلة:
- الخطة الحالية (512MB RAM, 20GB) غير كافية
- npm install يفشل أو يقتل العملية

### الحل:
- ترقية إلى $5/شهر (1GB RAM, 40GB)

### الخطوات:
1. خذ Snapshot
2. رقي الخطة (أو أنشئ Instance جديد)
3. أعد تثبيت المكتبات

### النتيجة:
- ✅ كل المكتبات تثبت بنجاح
- ✅ المشروع يشتغل كامل
- ✅ أداء أفضل

**الزيادة في السعر: +$1.50/شهر فقط!**

---

## 🔗 روابط مهمة:

- AWS Lightsail Console: https://lightsail.aws.amazon.com/
- Lightsail Pricing: https://aws.amazon.com/lightsail/pricing/
- Lightsail Documentation: https://docs.aws.amazon.com/lightsail/

---

**جاهز؟ ابدأ بالخطوة 1: خذ Snapshot! 🚀**
