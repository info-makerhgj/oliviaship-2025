# ✅ دليل التحقق قبل التشغيل

## 📋 قائمة التحقق

### 1. ✅ المتطلبات الأساسية
- [x] Node.js مثبت (v22.19.0 موجود)
- [x] npm موجود
- [x] الحزم مثبتة (`node_modules` موجود)

### 2. ⚙️ ملف `.env`
تأكد من وجود المتغيرات التالية في `.env`:

```env
# إعدادات الخادم
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# قاعدة البيانات (MongoDB)
MONGODB_URI=mongodb://localhost:27017/yemen-delivery
# أو إذا كنت تستخدم Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/yemen-delivery

# مفتاح JWT (مهم جداً!)
JWT_SECRET=your-super-secret-key-change-this-12345

# إعدادات البريد (اختياري)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# إعدادات Stripe (اختياري)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...

# إعدادات ScraperAPI (اختياري)
SCRAPERAPI_KEY=your-key
```

### 3. 🗄️ قاعدة البيانات MongoDB

#### أ) MongoDB محلي:
```bash
# تأكد من تشغيل MongoDB
# على Windows:
# ابحث عن "MongoDB" في قائمة ابدأ أو
# افتح Command Prompt كـ Administrator:
mongod
```

#### ب) MongoDB Atlas (السحابي):
- لا تحتاج تشغيل شيء محلياً
- فقط ضع رابط الاتصال في `MONGODB_URI`

### 4. 🔍 فحص الاتصال بقاعدة البيانات

قبل التشغيل، يمكنك التحقق من الاتصال:

```bash
# تشغيل السيرفر فقط لاختبار الاتصال
npm run server
```

يجب أن ترى:
```
✅ Connected to MongoDB
🚀 Server running on port 5000
```

### 5. 📦 إنشاء بيانات تجريبية (اختياري)

إذا أردت بيانات تجريبية:

```bash
node server/seed.js
```

هذا سينشئ:
- حساب مدير: `admin@example.com` / `admin123`
- حساب عميل: `customer@example.com` / `customer123`

---

## 🚀 تشغيل المشروع

### الطريقة الأولى: تشغيل كل شيء معاً
```bash
npm run dev
```

هذا سيشغل:
- Backend على http://localhost:5000
- Frontend على http://localhost:5173

### الطريقة الثانية: تشغيل كل واحد على حدى

**Terminal 1:**
```bash
npm run server
```

**Terminal 2:**
```bash
npm run client
```

---

## 🧪 اختبار التشغيل

### 1. اختبار Backend:
افتح المتصفح على:
```
http://localhost:5000/api/health
```

يجب أن ترى:
```json
{
  "status": "OK",
  "message": "Server is running"
}
```

### 2. اختبار Frontend:
افتح المتصفح على:
```
http://localhost:5173
```

يجب أن ترى واجهة الموقع.

### 3. اختبار قاعدة البيانات:
افتح Terminal جديد واكتب:
```bash
# إذا كان MongoDB محلي:
mongosh

# ثم:
use yemen-delivery
show collections
```

---

## ⚠️ المشاكل الشائعة

### مشكلة: `MongoDB connection error`
**الحل:**
1. تأكد من تشغيل MongoDB
2. تحقق من `MONGODB_URI` في `.env`
3. إذا كان Atlas، تأكد من السماح لـ IP بالوصول

### مشكلة: `Port 5000 already in use`
**الحل:**
1. غير `PORT` في `.env` إلى رقم آخر (مثل 3000)
2. أو أغلق البرنامج الذي يستخدم المنفذ

### مشكلة: `JWT_SECRET is required`
**الحل:**
1. تأكد من وجود `JWT_SECRET` في `.env`
2. يجب أن يكون نص طويل وعشوائي

### مشكلة: `Cannot find module`
**الحل:**
```bash
npm install
```

---

## 📊 ملخص المسارات الرئيسية

### المسارات العامة (لا تحتاج تسجيل دخول):
- `GET /api/health` - فحص حالة السيرفر
- `GET /api/orders/track/:orderNumber` - تتبع طلب
- `POST /api/products/validate-url` - التحقق من رابط
- `GET /api/pos/public` - نقاط البيع العامة
- `GET /api/pos/code/:code` - نقطة بيع بالكود

### المسارات المحمية (تحتاج تسجيل دخول):
- جميع المسارات الأخرى تحتاج `Authorization: Bearer <token>`

### المسارات الخاصة بالمدير:
- جميع المسارات في `/api/users`
- معظم المسارات في `/api/agents`
- `/api/payments/stats`
- `/api/wallet/*` (معظمها)

---

## 🎯 الخطوات التالية بعد التشغيل

1. ✅ افتح http://localhost:5173
2. ✅ سجل حساب جديد أو استخدم:
   - Admin: `admin@example.com` / `admin123`
   - Customer: `customer@example.com` / `customer123`
3. ✅ جرب إنشاء طلب
4. ✅ جرب السلة الذكية
5. ✅ تحقق من لوحة التحكم

---

## 📝 ملاحظات

- **الأمان**: في وضع التطوير، Rate Limiting معطل
- **CORS**: مسموح للوصول من `localhost:5173`
- **قاعدة البيانات**: البيانات محفوظة في MongoDB
- **الجلسات**: تستخدم JWT Tokens (لا تحتاج Redis)

---

تم إنشاء هذا الدليل لمساعدتك في التحقق من كل شيء قبل التشغيل! 🎉






