# 📱 دليل التشغيل السريع

## 🎯 ما هذا التطبيق؟

تطبيق جوال بسيط يعرض موقع Olivia Ship داخل WebView.
- يعمل على Android و iOS
- سهل التعديل والتخصيص
- جاهز للرفع على المتاجر

---

## ⚡ التشغيل السريع (5 دقائق)

### الخطوة 1: تثبيت الحزم
```bash
cd OliviaShip-Mobile-App
npm install
```

### الخطوة 2: تشغيل التطبيق
```bash
# للأندرويد
npm run android

# للآيفون (Mac فقط)
npm run ios
```

---

## ⚙️ التعديلات المهمة

### 1. تغيير عنوان الموقع
افتح `App.js` وغير:
```javascript
const WEBSITE_URL = 'http://192.168.1.111:5173';
```

إلى عنوان موقعك:
```javascript
const WEBSITE_URL = 'https://oliviaship.com';
```

### 2. تغيير اسم التطبيق
افتح `app.json` وغير:
```json
{
  "name": "OliviaShip",
  "displayName": "Olivia Ship"
}
```

---

## 📱 اختبار التطبيق

### على المحاكي:
```bash
npm run android
# أو
npm run ios
```

### على جهاز حقيقي:
1. وصل الجوال بالكمبيوتر
2. فعّل "USB Debugging" (Android)
3. شغل `npm run android`

---

## 🎨 تخصيص الألوان

في `App.js`، غير الألوان:
```javascript
// لون شاشة التحميل
color="#d946ef"  // غيره للون الذي تريد

// لون زر إعادة المحاولة
backgroundColor: '#d946ef'  // غيره للون الذي تريد
```

---

## 🚀 بناء التطبيق

### Android (APK):
```bash
cd android
./gradlew assembleRelease
```
الملف في: `android/app/build/outputs/apk/release/`

### iOS:
1. افتح Xcode
2. اختر Product > Archive
3. اتبع الخطوات

---

## ❌ حذف التطبيق

إذا لم يعجبك التطبيق، احذف المجلد:
```bash
cd ..
rmdir /s OliviaShip-Mobile-App
```

---

## 🆘 مشاكل شائعة

### المشكلة: "npm: command not found"
**الحل:** ثبت Node.js من [nodejs.org](https://nodejs.org)

### المشكلة: "SDK location not found"
**الحل:** ثبت Android Studio

### المشكلة: التطبيق لا يفتح الموقع
**الحل:** 
1. تأكد أن السيرفر يعمل
2. تأكد من عنوان الموقع في `App.js`
3. تأكد من الاتصال بالإنترنت

---

## 📞 الدعم

إذا احتجت مساعدة:
1. راجع `README.md`
2. ابحث في Google
3. اسأل مطور React Native

---

**بالتوفيق! 🚀**
